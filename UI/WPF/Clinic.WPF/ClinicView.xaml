<Window x:Class="Clinic.WPF.ClinicView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Clinic.WPF"
        mc:Ignorable="d"
        Title="Clinic" Height="350" Width="525">
    <Window.DataContext>
        <local:ClinicViewModel></local:ClinicViewModel>
    </Window.DataContext>
    <Grid>
        <StackPanel>
            <Button>Doctors</Button>
            <Button>Offices</Button>
            <Button>Patients</Button>
            <Button>Office Hours</Button>
            <Button>Appointments</Button>
            <Button>Examinations</Button>
            <Button>Doctor Activity</Button>
            <Button>Office Activity</Button>
            <Button>Clinic Activity</Button>
        </StackPanel>
    </Grid>
</Window>
